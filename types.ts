export interface TimerProps {
  initialTime: string;
}

export interface QuadrantProps {
  title: string;
  text: string;
  borderColor: 'pink' | 'orange';
}

export interface HeaderProps {
  logoUrl: string;
  iconsUrl: string;
}